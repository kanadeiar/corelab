<Window x:Class="SimpleWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SimpleWpf.ViewModels"
        xmlns:local="clr-namespace:SimpleWpf"
        mc:Ignorable="d"
        Title="{Binding Title}" 
        Height="500" Width="800" WindowStartupLocation="CenterScreen" ResizeMode="CanResizeWithGrip" Background="LightGreen">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Меню">
                <MenuItem Header="Выход" Cursor="Hand"
                    Command="{Binding CloseAppCommand}"/>
            </MenuItem>
            <MenuItem Header="Справка">
                <MenuItem Header="О программе ..." Cursor="Hand"
                    Command="{Binding ShowAboutCommand}"/>
            </MenuItem>
        </Menu>
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>                
                <Button Cursor="Hand" Height="40">
                    Тестовая кнопка
                </Button>
                <Button Cursor="Hand" Height="40">
                    Еще тестовая кнопка
                </Button>
                <Button Cursor="Hand" Height="40">
                    Третья тестовая кнопка
                </Button>
            </ToolBar>
        </ToolBarTray>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Text="Статус бар"/>
            </StatusBarItem>
        </StatusBar>
        <TabControl FontSize="14">
            <TabItem Header="Главная вкладка" Height="40">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Label Grid.Row="0" Grid.Column="0">
                        Поворот изображений
                    </Label>
                    <TextBox Grid.Row="1" Grid.Column="0" Margin="10,10,10,10"/>
                    <Grid Grid.Row="2" Grid.Column="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Button Name="cmdCancel" Grid.Row="0" Grid.Column="0" Padding="10" Margin="10,10,0,10" Cursor="Hand"
                            Click="cmdCancel_Click">
                            Отмена
                            </Button>
                        <Button Name="cmdProcess" Grid.Row="0" Grid.Column="2" Padding="10" Margin="0,10,10,10" Cursor="Hand"
                            Click="cmdProcess_Click">
                            Начать поворот изображений
                            </Button>
                    </Grid>
                </Grid>
            </TabItem>
            <TabItem Header="Дополнительная вкладка" Height="40">
                <Grid>

                </Grid>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
